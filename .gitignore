node_modules/
.env
.env.local
dist/
*.log
.DS_Store
data/*.sqlite*

# Vendored third-party skill reference docs used by the build agent,
# not Vouch402's own code, kept local only. See DECISION_LOG.md.
.agents/
.claude/

# Local Claude Code MCP tooling config (Base MCP), not part of the
# product itself. Doesn't hold secrets, just not something to ship in
# this repo's history for anyone who clones it.
.mcp.json
